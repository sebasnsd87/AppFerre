import Categoria from '../components/Categoria'

const Home = ({navigation,route}) => {
  
  return (
        <>
            <Categoria navigation={navigation} route={route}/>
        </>
  )
}

export default Home
